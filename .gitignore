# secrets — never commit
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*

# cloudflare local state
.wrangler/

# generated deploy config with REAL D1/KV ids, substituted from CI secrets at
# deploy time — must never be committed (the tracked wrangler.toml keeps its
# REPLACE_WITH_YOUR_* placeholders because this repo is public)
*.ci.toml

# dependencies
node_modules/

# build output
dist/
.astro/

# os / editor noise
.DS_Store
*.log

# local QA artefacts
.playwright-mcp/
*.png
!public/**/*.png

# secret-scan + audit artefacts (local only, never published)
scan-report.txt
audit-report.md
AUDIT-*.md
.seed-tmp/

# generated theme review artifact (large; reproduce via scripts/build-themes-contact-sheet.mjs)
themes-contact-sheet.png
